{
  "name": "@estateflow/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm exec eslint src --max-warnings=0",
    "typecheck": "pnpm exec tsc --project tsconfig.json --noEmit",
    "test": "node --test",
    "test:integration": "node -e \"console.log('No worker integration tests exist before EF-104.')\"",
    "build": "pnpm exec tsc --project tsconfig.json"
  },
  "dependencies": {
    "bullmq": "5.81.2"
  }
}
