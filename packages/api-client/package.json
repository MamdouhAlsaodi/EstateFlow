{
  "name": "@estateflow/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "pnpm exec tsc --project tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "build": "pnpm exec tsc --project tsconfig.json"
  }
}
